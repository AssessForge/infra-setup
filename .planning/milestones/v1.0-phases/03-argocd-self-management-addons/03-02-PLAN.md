---
phase: 03-argocd-self-management-addons
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/projects/AssessForge/gitops-setup/bootstrap/control-plane/addons/envoy-gateway/application.yaml
  - ~/projects/AssessForge/gitops-setup/bootstrap/control-plane/addons/envoy-gateway/manifests/envoy-proxy.yaml
  - ~/projects/AssessForge/gitops-setup/bootstrap/control-plane/addons/envoy-gateway/manifests/gateway-class.yaml
  - ~/projects/AssessForge/gitops-setup/bootstrap/control-plane/addons/envoy-gateway/manifests/gateway.yaml
  - ~/projects/AssessForge/gitops-setup/bootstrap/control-plane/addons/envoy-gateway/manifests/httproute-argocd.yaml
autonomous: true
requirements: [GW-01, GW-02, GW-03, GW-04, GW-05]

must_haves:
  truths:
    - "Envoy Gateway Application deploys both the Helm chart and raw Gateway API manifests via multi-source"
    - "OCI Flexible LB is configured at 10Mbps min/max via EnvoyProxy annotations"
    - "GatewayClass references the EnvoyProxy for OCI LB config"
    - "Gateway has HTTP (port 80) and HTTPS (port 443) listeners with TLS termination"
    - "HTTPRoute routes argocd.assessforge.com to ArgoCD Server on port 8080"
    - "HTTP requests redirect to HTTPS via 301"
  artifacts:
    - path: "~/projects/AssessForge/gitops-setup/bootstrap/control-plane/addons/envoy-gateway/manifests/envoy-proxy.yaml"
      provides: "EnvoyProxy with OCI LB annotations"
      contains: "oci-load-balancer-shape"
    - path: "~/projects/AssessForge/gitops-setup/bootstrap/control-plane/addons/envoy-gateway/manifests/gateway-class.yaml"
      provides: "GatewayClass linked to EnvoyProxy"
      contains: "gatewayclass-controller"
    - path: "~/projects/AssessForge/gitops-setup/bootstrap/control-plane/addons/envoy-gateway/manifests/gateway.yaml"
      provides: "Gateway with HTTP+HTTPS listeners and cert-manager annotation"
      contains: "cert-manager.io/cluster-issuer"
    - path: "~/projects/AssessForge/gitops-setup/bootstrap/control-plane/addons/envoy-gateway/manifests/httproute-argocd.yaml"
      provides: "HTTPRoute for ArgoCD + HTTP-to-HTTPS redirect"
      contains: "argocd-server"
  key_links:
    - from: "gateway-class.yaml"
      to: "envoy-proxy.yaml"
      via: "parametersRef"
      pattern: "name: assessforge-proxy"
    - from: "gateway.yaml"
      to: "gateway-class.yaml"
      via: "gatewayClassName"
      pattern: "gatewayClassName: assessforge-gateway-class"
    - from: "httproute-argocd.yaml"
      to: "gateway.yaml"
      via: "parentRefs"
      pattern: "name: assessforge-gateway"
---

<objective>
Create Envoy Gateway routing infrastructure with OCI Flexible Load Balancer and Gateway API resources.

Purpose: Envoy Gateway replaces ingress-nginx as the external traffic path. OCI Flexible LB (free tier, 10Mbps) serves as the entry point. Gateway API resources (GatewayClass, Gateway, HTTPRoute) route HTTPS traffic to ArgoCD with TLS termination at the Gateway.

Output: Updated Envoy Gateway Application with 3rd multi-source for raw manifests, plus 4 Gateway API manifest files in the manifests/ subdirectory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-argocd-self-management-addons/03-CONTEXT.md
@.planning/phases/03-argocd-self-management-addons/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Gateway API manifest files in envoy-gateway/manifests/</name>
  <files>
    ~/projects/AssessForge/gitops-setup/bootstrap/control-plane/addons/envoy-gateway/manifests/envoy-proxy.yaml
    ~/projects/AssessForge/gitops-setup/bootstrap/control-plane/addons/envoy-gateway/manifests/gateway-class.yaml
    ~/projects/AssessForge/gitops-setup/bootstrap/control-plane/addons/envoy-gateway/manifests/gateway.yaml
    ~/projects/AssessForge/gitops-setup/bootstrap/control-plane/addons/envoy-gateway/manifests/httproute-argocd.yaml
  </files>
  <read_first>
    - ~/projects/AssessForge/gitops-setup/bootstrap/control-plane/addons/envoy-gateway/application.yaml
    - .planning/phases/03-argocd-self-management-addons/03-RESEARCH.md
  </read_first>
  <action>
Create the `manifests/` subdirectory inside `addons/envoy-gateway/` and add 4 files:

**1. envoy-proxy.yaml** — EnvoyProxy custom resource with OCI LB annotations (per D-04, GW-02, GW-05):
```yaml
# EnvoyProxy — configuracao do Load Balancer OCI (free tier, 10Mbps)
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: EnvoyProxy
metadata:
  name: assessforge-proxy
  namespace: envoy-gateway-system
spec:
  provider:
    type: Kubernetes
    kubernetes:
      envoyService:
        annotations:
          oci.oraclecloud.com/load-balancer-type: "lb"
          service.beta.kubernetes.io/oci-load-balancer-shape: "flexible"
          service.beta.kubernetes.io/oci-load-balancer-shape-flex-min: "10"
          service.beta.kubernetes.io/oci-load-balancer-shape-flex-max: "10"
```
IMPORTANT: Bandwidth values are bare strings `"10"` — no units like "10Mbps". Both min and max at 10 pins to free tier.

**2. gateway-class.yaml** — GatewayClass linking to EnvoyProxy (per GW-03):
```yaml
# GatewayClass — declara Envoy Gateway como controlador do cluster
apiVersion: gateway.networking.k8s.io/v1
kind: GatewayClass
metadata:
  name: assessforge-gateway-class
spec:
  controllerName: gateway.envoyproxy.io/gatewayclass-controller
  parametersRef:
    group: gateway.envoyproxy.io
    kind: EnvoyProxy
    name: assessforge-proxy
    namespace: envoy-gateway-system
```

**3. gateway.yaml** — Gateway with HTTP + HTTPS listeners (per D-06, D-08, GW-03, CERT-04):
```yaml
# Gateway — listeners HTTP (redirect) e HTTPS (TLS termination)
apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: assessforge-gateway
  namespace: envoy-gateway-system
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  gatewayClassName: assessforge-gateway-class
  listeners:
  - name: http
    protocol: HTTP
    port: 80
    allowedRoutes:
      namespaces:
        from: All
  - name: https
    protocol: HTTPS
    hostname: argocd.assessforge.com
    port: 443
    allowedRoutes:
      namespaces:
        from: All
    tls:
      mode: Terminate
      certificateRefs:
      - kind: Secret
        name: argocd-tls
```
The `cert-manager.io/cluster-issuer: letsencrypt-prod` annotation causes cert-manager to auto-create a Certificate for the hostname, populating the `argocd-tls` Secret. Both listeners use `allowedRoutes.namespaces.from: All` to allow cross-namespace HTTPRoute attachment without ReferenceGrant.

**4. httproute-argocd.yaml** — HTTPRoute for ArgoCD + HTTP-to-HTTPS redirect (per D-06, GW-04):
```yaml
# HTTPRoute — rota HTTPS para o ArgoCD Server (porta 8080, modo insecure)
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: argocd-https
  namespace: argocd
spec:
  parentRefs:
  - name: assessforge-gateway
    namespace: envoy-gateway-system
    sectionName: https
  hostnames:
  - argocd.assessforge.com
  rules:
  - backendRefs:
    - name: argocd-server
      namespace: argocd
      port: 8080
---
# HTTPRoute — redireciona HTTP para HTTPS (301)
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: argocd-http-redirect
  namespace: argocd
spec:
  parentRefs:
  - name: assessforge-gateway
    namespace: envoy-gateway-system
    sectionName: http
  hostnames:
  - argocd.assessforge.com
  rules:
  - filters:
    - type: RequestRedirect
      requestRedirect:
        scheme: https
        statusCode: 301
```
ArgoCD Server runs with `--insecure` (configured in Plan 01), so the backend is plain HTTP on port 8080.

Use Portuguese comments for file headers as shown above.
  </action>
  <verify>
    <automated>test -f ~/projects/AssessForge/gitops-setup/bootstrap/control-plane/addons/envoy-gateway/manifests/envoy-proxy.yaml && test -f ~/projects/AssessForge/gitops-setup/bootstrap/control-plane/addons/envoy-gateway/manifests/gateway-class.yaml && test -f ~/projects/AssessForge/gitops-setup/bootstrap/control-plane/addons/envoy-gateway/manifests/gateway.yaml && test -f ~/projects/AssessForge/gitops-setup/bootstrap/control-plane/addons/envoy-gateway/manifests/httproute-argocd.yaml && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - envoy-proxy.yaml contains `apiVersion: gateway.envoyproxy.io/v1alpha1`
    - envoy-proxy.yaml contains `oci-load-balancer-shape-flex-min: "10"`
    - envoy-proxy.yaml contains `oci-load-balancer-shape-flex-max: "10"`
    - envoy-proxy.yaml does NOT contain "Mbps" anywhere
    - gateway-class.yaml contains `controllerName: gateway.envoyproxy.io/gatewayclass-controller`
    - gateway-class.yaml contains `name: assessforge-proxy` in parametersRef
    - gateway.yaml contains `cert-manager.io/cluster-issuer: letsencrypt-prod`
    - gateway.yaml contains `hostname: argocd.assessforge.com`
    - gateway.yaml contains `name: argocd-tls` in certificateRefs
    - gateway.yaml contains `from: All` for both listeners
    - httproute-argocd.yaml contains `name: argocd-server` as backend
    - httproute-argocd.yaml contains `port: 8080`
    - httproute-argocd.yaml contains `statusCode: 301` for redirect
    - httproute-argocd.yaml contains `sectionName: https` and `sectionName: http`
  </acceptance_criteria>
  <done>All 4 Gateway API manifest files exist in addons/envoy-gateway/manifests/ with correct OCI LB annotations, GatewayClass-to-EnvoyProxy linkage, Gateway with TLS termination and cert-manager annotation, and HTTPRoute routing to ArgoCD Server on port 8080.</done>
</task>

<task type="auto">
  <name>Task 2: Add third multi-source entry to Envoy Gateway Application</name>
  <files>~/projects/AssessForge/gitops-setup/bootstrap/control-plane/addons/envoy-gateway/application.yaml</files>
  <read_first>
    - ~/projects/AssessForge/gitops-setup/bootstrap/control-plane/addons/envoy-gateway/application.yaml
  </read_first>
  <action>
Update the existing Envoy Gateway Application manifest to add a third `sources` entry (per D-12) that points to the raw `manifests/` directory. This tells ArgoCD to apply the Gateway API YAML files (EnvoyProxy, GatewayClass, Gateway, HTTPRoute) alongside the Helm chart.

Add this as the third entry in the `sources` list, after the existing values ref and Helm chart entries:
```yaml
  - repoURL: 'https://github.com/AssessForge/gitops-setup'
    targetRevision: 'main'
    path: 'bootstrap/control-plane/addons/envoy-gateway/manifests'
```

The full `sources` list should be:
1. `repoURL: https://github.com/AssessForge/gitops-setup` with `ref: values` (existing)
2. `chart: gateway-helm` from `oci://docker.io/envoyproxy` at `1.4.0` (existing)
3. `repoURL: https://github.com/AssessForge/gitops-setup` with `path: bootstrap/control-plane/addons/envoy-gateway/manifests` (NEW)

Keep all other content unchanged (metadata, syncPolicy, destination).
  </action>
  <verify>
    <automated>grep -c "path: 'bootstrap/control-plane/addons/envoy-gateway/manifests'" ~/projects/AssessForge/gitops-setup/bootstrap/control-plane/addons/envoy-gateway/application.yaml | xargs test 1 -eq</automated>
  </verify>
  <acceptance_criteria>
    - application.yaml has exactly 3 entries under `sources:`
    - Third source contains `path: 'bootstrap/control-plane/addons/envoy-gateway/manifests'`
    - Third source has `targetRevision: 'main'`
    - Third source has `repoURL: 'https://github.com/AssessForge/gitops-setup'`
    - Existing sources (values ref and Helm chart) are unchanged
    - `targetRevision: '1.4.0'` still present for the Helm chart source
  </acceptance_criteria>
  <done>Envoy Gateway Application uses multi-source with 3 entries: values ref, Helm chart (gateway-helm 1.4.0), and raw manifests directory for Gateway API resources.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet -> OCI LB -> Envoy Gateway | External untrusted traffic enters cluster |
| Envoy Gateway -> ArgoCD Server | Internal plain HTTP (TLS terminated at Gateway) |
| HTTP listener (port 80) | Only used for ACME challenges and HTTPS redirect |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Tampering | HTTP listener open to all namespaces | accept | Required for cert-manager HTTP-01 challenges; HTTP listener only redirects to HTTPS (no real traffic served) |
| T-03-08 | Information Disclosure | Plain HTTP ArgoCD traffic | mitigate | HTTP-to-HTTPS 301 redirect ensures all user traffic is encrypted; ArgoCD backend is internal-only (ClusterIP) |
| T-03-09 | Denial of Service | OCI LB bandwidth | accept | 10Mbps free tier limit is a natural throttle; sufficient for admin UI traffic |
| T-03-10 | Spoofing | Gateway hostname | mitigate | HTTPS listener locked to `argocd.assessforge.com`; TLS certificate validates domain ownership |
</threat_model>

<verification>
1. `ls ~/projects/AssessForge/gitops-setup/bootstrap/control-plane/addons/envoy-gateway/manifests/` shows 4 files
2. `grep "oci-load-balancer-shape-flex-min" ~/projects/AssessForge/gitops-setup/bootstrap/control-plane/addons/envoy-gateway/manifests/envoy-proxy.yaml` returns `"10"`
3. `grep -c "sources" ~/projects/AssessForge/gitops-setup/bootstrap/control-plane/addons/envoy-gateway/application.yaml` confirms sources list
4. `grep "path:" ~/projects/AssessForge/gitops-setup/bootstrap/control-plane/addons/envoy-gateway/application.yaml` shows manifests path
</verification>

<success_criteria>
- 4 Gateway API manifests exist in envoy-gateway/manifests/
- EnvoyProxy has OCI LB annotations with 10Mbps free tier bandwidth
- Gateway has HTTP+HTTPS listeners with cert-manager annotation
- HTTPRoute sends argocd.assessforge.com traffic to argocd-server:8080
- Application manifest has 3 sources including the manifests directory
</success_criteria>

<output>
After completion, create `.planning/phases/03-argocd-self-management-addons/03-02-SUMMARY.md`
</output>
